# PostgreSQL / PostGIS – copy to .env.local and set values
DB_HOST=124.29.204.2
DB_PORT=11080
DB_NAME=vehicle_tracking
DB_USER=grafana_user
DB_PASSWORD=Meg@123

# Table for /api/fences (default: fence). Use cherat_fences for original table.
# FENCES_TABLE=fence
