# PostgreSQL / PostGIS
# Copy to .env.local and set your values.
DB_HOST=124.29.204.2
DB_PORT=11080
DB_NAME=vehicle_tracking
DB_USER=grafana_user
DB_PASSWORD=Meg@123

# Table for /api/fences (map). Transform inserts into TRANSFORM_TABLE (default: fence).
FENCES_TABLE=fence